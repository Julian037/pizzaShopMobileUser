import { View, Text} from 'react-native'

const OrdersHistory = () => {
  return (
    <View style={{ fontSize: 30, alignItems: 'center'}}>
      <Text style={{fontSize: 30, }}>OrdersHistory Screen</Text>
    </View>
  )
}

export default OrdersHistory